# PII — never commit
data/raw/
data/teachers-private.json
data/*-private.json

# Full job data with school+contact details — never commit (only public split is shipped)
data/jobs.json
# data/jobs-pending.json — קומיט פעיל מאז 8.6.2026 (לוקדאון בוטל 19.5, staging.html זקוק לקובץ)

# Full teacher data with name+email+phone — never commit (only public split is shipped)
data/teachers.json
# data/teachers-pending.json — קומיט פעיל מאז 8.6.2026
data/fb-teachers.json
data/fb-teachers-2.json

# Working drafts
design-preview-atlas.html

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*~

# Python (merge.py / make-teachers.py local runs)
__pycache__/
*.pyc
.venv/
venv/

# Node (if added in future)
node_modules/

# Playwright
.playwright-mcp/

# Logs
*.log
